{
  "name": "@identity-connect/dapp-sdk",
  "version": "0.0.4",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "tsc -b tsconfig.build.json --clean",
    "lint": "prettier --check src",
    "lint:fix": "prettier --write src && eslint --cache ./src --fix",
    "test": "jest"
  },
  "dependencies": {
    "@identity-connect/api": "*",
    "@identity-connect/crypto": "*",
    "aptos": "^1.8.5",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "axios-mock-adapter": "^1.21.4",
    "jest": "^29.1.0",
    "ts-jest": "^29.1.0",
    "ts-node": "10.9.1",
    "typescript": "5.0.4"
  }
}
